#pragma once

// Default-on private M-009 pilot; no save flag or randomizer dependency.
void TryStartVisibleHiddenItemSparkles(void);

// The tracked pret/Cyan census peaks at 36 BG events (Celadon Game Corner).
// Oversized future tables fail closed until this contract is reviewed.
#define M009_MAX_BG_EVENTS 36
#define M009_SPARKLE_COOLDOWN 16
